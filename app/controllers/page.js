import Controller from '@ember/controller';


export default class PageController extends Controller {
  categories = [{id: 1, name: "cat"}, {id: 2, name: "dog"}, {id: 2, name: "really big crocodile"}]
}